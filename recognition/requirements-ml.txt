# Heavy ML dependencies — only needed to run the real models (YOLO detector,
# and later the screen classifier / MiDaS depth). The base validation package
# and demo run with just requirements.txt.
#
#   pip install -r requirements.txt -r requirements-ml.txt

ultralytics>=8.3      # YOLO11 detector (pulls torch, torchvision)

# Make/model training (recognition/training/) — run on a GPU.
torch>=2.2
torchvision>=0.17
timm>=1.0             # pretrained backbones
pandas>=2.0
scikit-learn>=1.3     # stratified split
